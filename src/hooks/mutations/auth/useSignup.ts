import { postSignup } from '@/api/auth';

export function useSignUp(callbacks?: any) {
  // return useMutation({
  //   mutationFn: postSignup,
  //   onError: (error) => {
  //     if (callbacks?.onError) callbacks.onError(error);
  //   },
  // });
}

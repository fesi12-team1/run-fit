import {
  Canvas,
  Controls,
  Meta,
  Primary,
  Story,
} from '@storybook/addon-docs/blocks';
import * as ButtonStories from './Button.stories';

<Meta of={ButtonStories} />

# Button

버튼은 사용자가 클릭하여 작업을 수행하거나 다른 페이지로 이동할 수 있는 클릭 가능한 요소입니다.

<Primary />

## Props

<Controls of={ButtonStories.Default} />

## Variants

다양한 버튼 스타일을 제공합니다.

<Canvas of={ButtonStories.Variants} />

## Sizes

버튼의 크기를 조절할 수 있습니다.

<Canvas of={ButtonStories.Sizes} />

## States

버튼의 다양한 상태입니다.

<Canvas of={ButtonStories.States} />

## Icon Buttons

아이콘만 있는 버튼입니다.

<Canvas of={ButtonStories.IconButtons} />

## Usage

```tsx
import { Button } from '@/components/ui/button';

// 기본 버튼
<Button>Click me</Button>

// 다양한 variants
<Button variant="destructive">Delete</Button>
<Button variant="outline">Cancel</Button>
<Button variant="ghost">Ghost</Button>

// 다양한 sizes
<Button size="sm">Small</Button>
<Button size="lg">Large</Button>

// 비활성화
<Button disabled>Disabled</Button>

// 아이콘과 함께
<Button>
  <PlusIcon className="w-4 h-4 mr-2" />
  Add Item
</Button>
```

## Guidelines

### When to use

- 사용자가 액션을 수행해야 할 때
- 폼 제출이나 다이얼로그 확인 등의 주요 액션
- 네비게이션 목적 (링크 대신)

### When not to use

- 단순한 텍스트 링크가 더 적절한 경우
- 너무 많은 버튼이 한 화면에 있을 때는 우선순위 고려

### Best practices

- 명확하고 액션 지향적인 라벨 사용
- 주요 액션에는 primary variant 사용
- 위험한 액션에는 destructive variant 사용
- 적절한 크기와 터치 영역 확보
